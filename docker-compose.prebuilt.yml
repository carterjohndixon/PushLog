# Override: use pre-built context from scripts/build-for-docker.sh
# Usage: docker compose -f docker-compose.staging.yml -f docker-compose.prebuilt.yml up -d --build
services:
  staging-app:
    build:
      context: .docker-build/prebuilt
      dockerfile: Dockerfile
