[package]
name = "pkg-compare"
version = "0.1.0"
edition = "2021"
description = "Fast package.json diff for deploy decisions"

[[bin]]
name = "pkg-compare"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
